# <Task name>

Replace this README with a description of your task. The learning agent will read this file to understand what it's being asked to solve, so make it actionable.

## What the task is

What's the question? What does a correct answer look like? What's hard about it?

## Where the data comes from

HuggingFace dataset, local files, API, etc. Include any auth requirements (env vars, hf-token).

## Answer format

Exact spec of what the learning agent's `answer` script must output. Be concrete — show an example. The agent reads this when designing its answer script, so anything ambiguous becomes a footgun.

## Scoring

How is reward computed? F1, accuracy, exact match, judge model, etc. What does a perfect score look like? What does a 0 mean?

## Domain notes

Anything the agent should know upfront — relevant background, common pitfalls, suggested first patterns to try. Keep it short; the agent will accumulate its own notes during training.
